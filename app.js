// app.js - To-Do Life Dashboard entry point
// ES Module: wired to DOMContentLoaded

/**
 * Top-level initialisation function.
 * Called once the DOM is fully parsed.
 * Subsequent tasks will fill in the widget initialisations here.
 */
export function init() {
  // Placeholder - full implementation in Task 9
  console.log('[Dashboard] init() called');
}

document.addEventListener('DOMContentLoaded', init);
